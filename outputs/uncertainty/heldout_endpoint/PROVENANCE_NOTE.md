# Provenance note

This endpoint-only contributor-bootstrap uses the independent occupant-record
held-out split and saved predictor bundles. It does not use EnergyPlus traces,
so the legacy begin/end-timestep alignment defect does not affect these results.

The endpoint panel sensitivity is a separate output of the corrected same-state
robustness run and should not be conflated with this held-out validation audit.
